# Todo 웹 앱 — 작업 조건 정리

> Vanilla JS로 만드는 Todo 웹 앱. 아래 조건을 모든 기능 구현/수정 시 항상 지킨다.

## 기술 스택
- **HTML / CSS / Vanilla JS만 사용** (라이브러리, 프레임워크 사용 금지)

## 파일 구성
- 다음 3개 파일로만 구성한다.
  - `index.html`
  - `style.css`
  - `app.js`

## 디자인 / UI
- 디자인은 **미니멀하고 깔끔하게**.
- UI는 **디자인과 생산성 측면에서 잘 배치**한다.

## 코드 스타일
- 변수명과 함수명은 **역할을 알 수 있도록 명확하게** 작성한다.
- 코드에는 **동작 방식을 이해할 수 있도록 주석**을 단다.

## 응답 방식
- 기능을 추가하거나 수정할 때는 **항상 `index.html`, `style.css`, `app.js` 전체 코드를 파일별로 나눠서** 보여준다.
- 모든 작업을 마친 후에는 **어떤 기능을 구현했는지 체크리스트**로 답변한다.

## 콘셉트
- **구글 캘린더 느낌**의 Todo 앱. 캘린더 형태로 일정을 보고, 추후 칸에 할 일을 얹는 방향.

## 진행 현황 (2026-06-02 기준)

### 완료
1. **기본 UI 레이아웃 (구글 캘린더 스타일)**
   - 상단 헤더: 로고(📅 Todo) / 오늘 버튼 / 이전·다음 화살표 / 기간 라벨 / 검색창 / 월·주·일 보기 전환 / 설정
   - 좌측 사이드바: 만들기 버튼 / 미니 달력 / '내 목록' 카테고리 라벨(디자인용)
   - 메인: 월간 달력 격자, 오늘=파란 원, 일요일=빨강, 다른 달=흐리게
2. **상단바 색상 조정** — 헤더 배경을 은은한 회색(`--color-header-bg: #f8f9fa`), 검색창은 흰색+테두리로 대비
3. **월/주/일 보기 실제 전환 구현**
   - 월: 6주 × 7일 격자 / 주: 7일 × 24시간 타임그리드 / 일: 1일 × 24시간 타임그리드
   - 이전·다음 이동이 보기 기준(±1달 / ±7일 / ±1일)으로 동작
   - 기간 라벨 자동 변경, 타임그리드 초기 스크롤 오전 8시 부근
   - 월 보기 날짜 클릭 → 일 보기 전환, 미니 달력 날짜 클릭 → 포커스 이동(+선택 표시)

### 아직 디자인용 자리(미구현)
- 검색창, 설정 버튼, 메뉴(☰) 버튼, '내 목록' 카테고리 체크박스, 만들기 버튼

### 다음 단계(예정)
- 실제 **할 일(Todo) 추가 / 표시 / 완료 / 삭제 / 저장(localStorage)** 기능
- 날짜·시간 칸에 할 일 얹기

## 핵심 코드 메모
- 상태: `focusDate`(기준 날짜), `viewMode`("month"|"week"|"day")
- 메인 렌더 진입점: `renderAll()` → 보기별 `renderMonthView()` / `renderTimeGridView(days)`
- 색상·간격은 `:root`의 CSS 변수로 관리
